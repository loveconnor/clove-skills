@AGENTS.md

# Claude Code compatibility

The canonical skills live in `.agents/skills`. Use the matching skill there whenever the task or `AGENTS.md` skill router calls for it. Read its complete `SKILL.md` before acting and resolve relative resource paths from that skill directory.
